# Pre-flight Checklist — Agent Eyes v0.10.0

**Service:** agent-eyes
**Version:** 0.10.0
**Release candidate:** final pull request SHA, recorded before tagging
**Strategy:** Immutable PyPI release through GitHub Trusted Publishing
**Owner:** Jelly Thomas
**Target window:** 2026-07-28 Asia/Jakarta, after every mandatory gate

## Code and review

- [ ] Release diff merged to `main` through a pull request.
- [x] Architecture and scope were frozen before implementation.
- [x] No broker, browser-specific core dependency, mandatory bridge, or new
  runtime package was added without benchmark evidence.
- [ ] Final architecture, performance, and security reviews have no open P0/P1/P2.

## Deterministic correctness and performance

- [ ] Full test suite passes three consecutive times with the bounded watchdog.
- [ ] Ruff, compilation, Bandit, and dependency audit pass.
- [ ] Known journey uses one call; discovery uses at most two.
- [ ] Full scans before the first mutation and activations are each at most one.
- [ ] Successful external-write journey dispatches exactly one external write and
  no cancellation/deadline schedule dispatches more than one.
- [ ] Foreground journeys perform zero shadow probes.
- [ ] Fixed orchestration sleeps are zero.
- [ ] `execute` output is at most 2 KiB, `observe_target` at most 4 KiB, and the
  platform-filtered catalog at most 16 KiB.
- [ ] Every named transaction boundary and 32 queued transactions pass cancellation,
  no-overlap, and no-post-deadline-mutation checks.
- [ ] 10,000 lifecycle repetitions retain no snapshots, provider references,
  workers, subscriptions, locks, resolver flights/leases/cache entries, and RSS
  growth is at most max(10 MiB, 5%).

## Build and exact artifact

- [ ] Two independent clean-tree wheels and normalized sdists are byte-identical.
- [ ] The wheel rebuilt from the sdist is byte-identical to the upload candidate.
- [ ] Twine metadata and archive-content checks pass.
- [ ] Isolated exact-wheel smoke proves version, 30/27 tool counts, the
  `observe_target` and `execute` schemas, compact output budgets, setup dry-run,
  readiness, MCP handshake, and bounded fast-path skill template.
- [ ] Runtime version, plugin version, wheel metadata, CLI, MCP server, generated
  skills, and installed skill all identify 0.10.0.

## Reference Mac and provenance

- [ ] Three warmups plus 30 exact-wheel native-accessibility runs pass median <=1
  second and p95 <=3 seconds.
- [ ] Evidence records OS, Python, browser, Agent Eyes version, wheel SHA-256, and
  provider phases.
- [ ] Reference-agent trace reports Agent Eyes, model, and client durations
  separately; its preloaded <=3-second median remains a stretch target.
- [ ] Deterministic benchmark JSON is checked in under `benchmarks/results/`, and
  exact tagged-wheel live JSON is attached to the GitHub release.

## Release and post-publish

- [ ] Hosted Linux Python 3.10–3.14, macOS, Windows, package, quality, and stress
  jobs pass on the exact merge SHA.
- [ ] Tag `v0.10.0` points to that exact `main` SHA.
- [ ] GitHub release is published and trusted publishing completes successfully.
- [ ] PyPI exposes the expected wheel and sdist with matching SHA-256 provenance.
- [ ] The previous local uv-tool installation is removed only after source/artifact
  gates pass; the exact published wheel is then installed and rechecked.
- [ ] Changed MCP clients are configured by `agent-eyes setup` and restarted.

## Rollback and sign-off

- [x] No database, schema, or destructive data migration exists.
- [x] Rollback/yank procedure is documented in the release notes.
- [ ] Release owner approves the immutable tag and publish action after all gates.
- [ ] Release owner remains available through PyPI and local-install verification.
